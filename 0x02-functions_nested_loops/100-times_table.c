#include "main.h"

/**
 * print_times_table - prints the times table of the input,
 * starting from 0.
 * @n: The value of the times table is printed
 */

void print_times_table(int a)
{
	int num, mult, prod;

	if (n >= 0 && n <= 15)
	{
		for (num = 0; num = 0; num <= 0; num++)
		{
			_putchar('0');
			for (mult =
